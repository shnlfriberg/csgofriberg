# 历史提示词说明

以下是第三阶段开始前的任务提示词存档。第三阶段已执行并创建上游 PR；后续任务请先阅读 [最新发布验收记录](turtle-soup-release-validation.md) 和 [交接文档](turtle-soup-handoff.md)，只处理其中尚未验证或受阻的事项，不要重复创建 PR 或重做已完成验收。

# 给下一位 agent 的提示词（存档）

请继续开发“弗一把海龟汤”，接手第三阶段：完整发布验收、必要修复和向上游提交 PR。

## 当前状态

- 工作目录：`F:\弗一把新模式弗龟汤`。
- 功能分支：`feat/turtle-soup`。交接前最后一个功能/样式提交是 `20f2034`，后面还有交接文档提交；以实际 Git 记录为准。
- 第一阶段后端与本地环境、第二阶段前端接入及交互调整已经完成。用户已正常完成一局游戏并明确确认测试完成，不要重新开展第二阶段，也不要重新询问已确定的玩法和布局偏好。
- 尚未部署、推送或创建 PR。本次可以在验收和必要修复完成后，使用 fork + 功能分支向上游创建 PR，并跟进 CI；不要直接推送上游 main，不要自行合并、部署、发布镜像或打发布标签。部署待维护者合并后另行安排。

## 先做什么

1. 先检查当前 Git 状态、分支和近期提交，保留用户未提交的改动。
2. 阅读 `docs/turtle-soup-handoff.md`、`docs/turtle-soup-api.md` 和适用的 `AGENTS.md`。AGENTS.md 被忽略规则覆盖，查找要包含忽略文件与父目录。交接文档包含最终行为、源码入口、启动方式、测试证据与逐项第三阶段验收清单。
3. 只读检查现有服务；可用就直接使用。不要重建本地环境、重装工具、重新导入数据或重复实现后端。
4. 原 stash `ba48ddc3660812141bfff3c522f498bc764502e5` 已应用并完善提交，必须保留备份，**不得再次 apply/pop**。

## 本地环境与数据保护

- 前端：`http://localhost:5173`；后端：`http://localhost:3000`，健康接口 `/api/health`。
- SQLite：`.local-data/development.sqlite3`；Redis：`127.0.0.1:16379`，开发前缀 `csgofriberg-local:`。
- 现有数据源是 `E:\Users\Administrator\Downloads\players.json`，668 条选手，其中 646 启用、22 禁用，已导入完成。不要导入旧快照，也不要覆盖现有账号、用户刚完成的游戏或其他历史记录。
- Node/pnpm/Redis 已在 `.local-tools`。按需执行：

```powershell
Set-Location -LiteralPath 'F:\弗一把新模式弗龟汤'
pwsh -NoProfile -File scripts/local.ps1 -Action start
pwsh -NoProfile -File scripts/local.ps1 -Action test
pwsh -NoProfile -File scripts/local.ps1 -Action build
```

`start` 仅在服务需要启动时运行。不要例行 stop/start：本地 Redis 不持久化，停止会影响活跃对局。测试脚本使用独立数据库和 Redis 前缀。不得提交或输出密钥、测试账号凭据、数据库、完整数据文件；`.local-tools`、`.local-data`、`.local-logs`、`.local-scripts` 都不提交。

## 必须保留的产品约定

- 每局 18 次属性提问；有效提问解锁一次免费猜名，机会不累积，猜错后锁定。第 18 问之后保留最后一次猜名，猜错才判负。认输记负，退出/重开不计战绩。30 分钟无有效写操作过期；读取不续期。
- 属性判断、次数、锁定、胜负由服务端决定。网络不确定时复用同一 requestId、version 和请求体，不得重建请求导致重复扣次数；409、刷新、并发标签页按接口契约同步。
- 战队和国家／地区输入文字后才显示匹配候选，选中只填入不提问；位置和现役／退役直接下拉选择。候选来自本局快照，翻译显示但提交原始值，保留无战队入口和输入法回车保护。
- 首页宽屏五个入口一行，手机五个扁平入口纵向排列。首页规则含海龟汤，大厅及游戏规则按钮与弹窗居中。
- 游戏记录最新置顶、原始编号不变；历史回放按发生顺序展示快照。记录区独立滚动，新结果不带动整页、不撑长页面，结算在窗口顶部。
- 桌面两栏等高且适度紧凑，1080p 100% 缩放能显示全部提问、认输、规则和底部猜名栏；窗口底部到昵称框留白已按用户要求缩小。不要退回早期的过长布局。
- 海龟汤战绩与普通玩法独立；海龟汤获胜提问数和猜名次数不可混用。中／英／日、light／blast 均需保持可用。

## 本次需要完成

1. 对当前代码运行完整测试和构建。已有最后测试基线为后端 203 项、前端 160 项；末尾 CSS 改动另有构建和 30 组浏览器布局检查，不能说完整测试已在最后 CSS 提交后跑过。
2. 按交接文档补齐专用账号登录/登出、访客战绩归并、隐藏排名、个人与后台回放的端到端验收；保留现有账号与历史，使用隔离测试身份。
3. 验证真实设备/跨浏览器、软键盘及中日输入法、触控、长时间多标签页和弱网恢复、真实 30 分钟过期。已有 Edge 模拟和一局用户试玩不能替代这些专项。缺少设备或外部条件时明确记录未验证项，并继续独立工作；确需用户参与时只提出具体缺失步骤。
4. 在隔离的 PostgreSQL + Redis + Linux/容器环境验证旧数据迁移、重复迁移、生产镜像启动、静态资源/SPA 深链、PoW、身份 Cookie、代理与缓存兼容。不得覆盖现有开发数据库或启动配置去连接生产数据，不得通过删除认证或放宽规则让验收通过。
5. 对发现的问题做必要修复，补适当测试，再完成最终回归与截图检查。不要为做阶段三擅自扩大玩法范围。旧本地验收脚本可能有过时选择器和布局断言，使用前先检查。
6. 更新交接文档，逐项写明通过/未验证/阻塞、环境、证据和剩余事项；审核提交不含敏感信息或无关缓存，并提交到当前功能分支。
7. 验收后核对 GitHub 身份、权限、远端和上游基线。早期 `origin` 为 `https://github.com/shnlfriberg/csgofriberg.git`，当时账号仅有上游读取权限，因此使用 fork + 功能分支流程；不要强推或覆盖无关历史。创建面向上游的 PR，标题/正文覆盖整个海龟汤最终实现、迁移及真实验证，跟进 CI。若权限或外部条件确实阻塞，交付具体阻塞、现有提交及可审阅的 PR 草稿，不声称已经完成。

完成后给出提交、验证结果、未验证范围、PR 链接及实际 CI 状态。除确需用户决定或参与的事项外，自主完成已授权的验证、修复与收尾，不反复确认已经确定的产品规则。
